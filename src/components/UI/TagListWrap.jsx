import { styled } from '@mui/material';

const TagListWrap = styled('ul')({
  width: '100%',
  minHeight: 32,
  margin: '0',
  padding: 0,
  border: 'none',
  color: '#ffffff',
});

export default TagListWrap;
