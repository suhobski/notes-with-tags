import { styled } from '@mui/material';

const Tag = styled('li')({
  position: 'relative',
  display: 'inline-block',
  margin: '0 0.5rem 0.5rem 0',
  padding: '0 0.25rem',
  listStyleType: 'none',
  borderRadius: 4,
  background: '#5a5a65',
  color: '#ffffff',
  cursor: 'pointer',
});

export default Tag;
