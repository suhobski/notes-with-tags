import { Card, styled } from '@mui/material';

const ModalWindow = styled(Card)({
  margin: '0.5rem',
  padding: '1rem',
  borderRadius: 8,
  background: '#ffffff',
  color: '#5a5a65',
  zIndex: 60,
});

export default ModalWindow;
