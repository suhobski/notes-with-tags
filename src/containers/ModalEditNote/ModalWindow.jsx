import { Card, styled } from '@material-ui/core';

const ModalWindow = styled(Card)({
  margin: '0.5rem',
  padding: '1rem',
  borderRadius: 8,
  background: '#ffffff',
  color: '#5a5a65',
  zIndex: 60,
});

export default ModalWindow;
