import { Box, styled } from '@mui/system';

const TagsFieldHeader = styled(Box)({
  display: 'flex',
  justifyContent: 'space-between',
  alignItems: 'center',
  paddingBottom: 8,
});

export default TagsFieldHeader;
