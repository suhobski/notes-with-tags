import { Card } from '@mui/material';
import { styled } from '@mui/material/styles';

const CreateNoteFormWrap = styled(Card)({
  padding: '1rem 0.5rem',
  color: '#5a5a65',
  background: '#ffffff',
  borderRadius: '0.75rem',
  zIndex: 0,
});

export default CreateNoteFormWrap;
