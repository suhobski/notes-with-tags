import { Card, styled } from '@mui/material';

const BoardWrap = styled(Card)({
  padding: '1rem 0.5rem',
  borderRadius: 12,
  background: '#ffffff',
  color: '#5a5a65',
  zIndex: 0,
});

export default BoardWrap;
